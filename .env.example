# docker-compose.yaml에서 선택적으로 참조하는 로컬 시크릿 - 필요하면 이 파일을 .env로 복사해서 채우기
# .env는 .gitignore 처리되어 있어 커밋되지 않음. 값을 안 채워도 전체 시스템은 정상 기동됨
# (구글 로그인, 실제 Toss 결제 승인만 비활성).

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
TOSS_SECRET_KEY=
